pipeline {
        agent any

        stages {
                stage("Build") {
                        steps {
                                sh 'cargo build'
                                sh 'cargo test -- test-threads=1'
                        }
                }
        }
}
